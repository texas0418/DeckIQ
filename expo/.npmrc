# lucide-react-native@0.475.0 declares react <=18 as a peer while this app is
# on react 19 (worked fine under bun, which is lax about peers). Keep npm lax
# too so `npm install` / `npm ci` resolve without EOVERRIDE noise.
legacy-peer-deps=true
